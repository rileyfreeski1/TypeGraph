# Data driven Graph drawing Util

## To run on command line
```bash
cd api
npm install
npm run build:typegraph
npm run start:cli <number-of-iterations>
```

## To run as standalone API
```bash
npm install
npm run build:typegraph
npm run start:api
```