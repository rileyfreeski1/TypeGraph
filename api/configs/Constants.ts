// Circle constants
export const INTERVAL = 0.5;

export const LATERAL_OFFSET = 93.037;
export const VERTICAL_OFFSET = 150.87;
export const RADIUS = 177.25;

export const THRESHOLD = 0.51;
export const MODULUS = 15;

export const squareFunc = (val: number): number => Math.pow(val, 2);

// Chart constants

export const OPTIONS = {
  selector: '#chart',
  container: '<div id="container"><div id="chart"></div></div>'
};